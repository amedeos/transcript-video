"""Transcript Video — ASR + diarization pipeline built on whisperX.

Two console entry points:
- ``transcribe-video``: full ASR + alignment + diarization pipeline.
- ``transcript-to-md``: re-render Markdown from a previously produced JSON.
"""

__version__ = "0.1.0"
